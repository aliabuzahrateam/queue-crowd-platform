export const createRuleService = async (data: any) => {
  // DB logic to create rule
};

export const listRulesService = async () => {
  // DB logic to list rules
};

export const updateRuleService = async (id: number, data: any) => {
  // DB logic to update rule
}; 