export const createSessionService = async (data: any) => {
  // DB logic to create session
};

export const listSessionsService = async () => {
  // DB logic to list sessions
};

export const revokeSessionService = async (id: number) => {
  // DB logic to revoke session
}; 